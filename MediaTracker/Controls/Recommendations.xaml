<UserControl x:Class="MediaTracker.Controls.Recommendations"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Warning message, visible only if DataGrid is empty  -->
        <Label Name="LblNoRecs"
               Content="You have no recommendations"
               Visibility="Collapsed" />

        <DataGrid Name="DataGridRecs"
                  Grid.Row="1"
                  Height="Auto"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Stretch"
                  AutoGenerateColumns="True"
                  AutoGeneratedColumns="DataGridRecs_OnAutoGeneratedColumns"
                  AutoGeneratingColumn="DataGridRecs_OnAutoGeneratingColumn"
                  CanUserAddRows="False"
                  Grid.IsSharedSizeScope="True"
                  IsReadOnly="True"
                  MouseDoubleClick="DataGridRecs_OnMouseDoubleClick">

            <DataGrid.Columns>

                <!--  Delete button  -->
                <DataGridTemplateColumn Width="30"
                                        CanUserResize="False"
                                        Header="X">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="RemoveItem"
                                    CommandParameter="{Binding Path=Title}"
                                    Content="✘"
                                    Tag="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>
